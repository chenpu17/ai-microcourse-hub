version: "3.9"

services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "file:./prisma/dev.db"
      ADMIN_PASSWORD: "microwave-admin"
    volumes:
      - ./prisma:/app/prisma
